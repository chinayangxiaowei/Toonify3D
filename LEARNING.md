# PaperRoute 实现思路笔记

拆解这个 3D 网页送报游戏背后的实现思路与设计取舍。

---

## 1. 弧形街道：里程 + 横向偏移的坐标系

街道是一条缓慢 S 形弯曲的路线，但所有玩法逻辑都按"直线"写：

- 中心线函数 `centerX(z)`：两个不同频率的正弦叠加，形成自然的缓弯；
- `heading(z)` 求道路在该里程处的切线角；
- 任意物件的世界坐标 = `roadPoint(z, offset)` —— "沿路线走 z 米，再沿道路法向偏移 offset 米"；
- 路面、人行道、路缘用**带状几何（ribbon）**：沿中心线每隔 0.5m 取左右边界点对，连成三角带。

**思路要点**：弯道不需要样条系统。换一个"里程 + 横偏"坐标系之后，碰撞检测、邮箱距离、雷达 HUD 等全部保持一维直线逻辑，只在最终摆放时做一次坐标变换。这是"用坐标系变换消除问题复杂度"的典型例子。

## 2. 房屋：少量户型族 + 程序化变化

- 只有 7 个基础户型（4 普通 + 3 豪宅），打包在一个 GLB 里按地块轮换；
- 每栋房实例化时克隆，并做**种子化 HSL 偏移**——但只改"墙板"类材质（按材质名前缀识别），门窗、门廊、屋顶保持原色；
- 几何顶点带 paint mask / 顶点色属性，标记哪些区域参与变色和罩染。

**思路要点**：手工资产 × 程序化变化 = 用极少素材做出"每户不同但风格统一"的街区。关键是变色要有选择（按材质角色），整体染色会立刻破坏美术的统一性。

## 3. 手绘油画风：画出来的大关系 + 一层统一肌理

这种风格几乎不靠模型精度，靠四层"画法"：

1. **水粉罩染着色器**：`onBeforeCompile` 在标准材质片元里注入世界坐标噪声，扰动 albedo，纯色墙面立刻有了颜料堆叠的纸面肌理。注意材质 clone 后补丁丢失，要重新注入。
2. **手绘天空**：等距全景图直接做 `scene.background` 兼 `environment`（给所有材质一层天光反射），贴图接缝用 `backgroundRotation` 转到玩家背后。
3. **远景小镇环幕**：alpha 剪影长图贴在开口圆柱内壁（BackSide）绕街道一圈，平铺 2 次并调整旋转角，把仅剩的接缝藏进街道两侧的视线死角。
4. **交叉面片树**：2~3 片十字交叉的面片贴手绘树叶 alpha 图，远看蓬松、性能为零。

光照配合：暖色低角度平行光（清晨）+ 半球光补环境色 + ACES 色调映射 + 淡雾融到地平线。

**思路要点**：风格化的本质是统一——统一的肌理（罩染）、统一的色温（晨光）、统一的大气（雾）。瑕疵不求消除，求藏到玩家看不到的地方（接缝永远在身后或侧面）。

## 4. 声音：两个音频文件 + 全套合成音效

整个游戏只有 2 个音频文件（背景旋律、玻璃碎裂采样），其余音效全部 **WebAudio 实时合成**：

- 音调用 `f = 440 × 2^((midi-69)/12)` 从 MIDI 音高换算；
- 短旋律（投递成功、失误）= 若干"音符"声部叠加，各自带 delay / duration / volume / 波形（方波、三角波）；
- 噪声类音效（投掷的"呼"、撞车）= 白噪声 BufferSource + 带通/低通滤波扫频；
- 每个声部走指数包络：快速 attack 到峰值，再衰减到 0，干净利落；
- 同类音效 70ms 去抖，防止一帧重复触发；
- AudioContext 必须在用户手势里创建/恢复（浏览器自动播放策略），采样提前 fetch + `decodeAudioData` 解码缓存。

**思路要点**：合成音效参数全在代码里，风格天然统一、随改随听，比管理一堆音频文件轻得多。"投递成功是上行音、失误是下行音"这种音高走向设计，让玩家不看屏幕也能听出结果。

## 5. 玩法：稳定手感来自解析解

- **抛物线用解析解而非手感调参**：由起点、目标点、飞行时间反推初速度（`v = Δx/t`，`vy = Δy/t + gt/2`），报纸永远精确落向目标，命中判定只是距离阈值——手感稳定，不会有"明明瞄准了却飞了"的挫败。
- **连击经济**：100 × min(连击, 5)，而断连的来源很多（撞车、错过订户、投错房、砸订户窗户）——风险无处不在，连击才值钱。
- **一个机制两种情绪**：砸窗户对非订户是 +400 恶作剧奖励，对订户是直接流失——同一动作，按目标身份给出完全相反的反馈，玩家自己会琢磨出"哪些房子可以砸"。
- **障碍可背板**：狗守固定地块、24m 激活、可用报纸分散 5 秒；坑洼垃圾桶种子固定摆放。游戏是"背板 + 执行"型，而非纯反应型，这才能支撑排行榜竞争。

## 6. 确定性种子：mulberry32

街区布局（订户挑选、户型轮换、树/栅栏/障碍位置）全部由 `mulberry32(seed)` 决定，同一种子永远生成同一条街。

**思路要点**：确定性随机同时解决四件事——bug 可复现、改动前后截图可对比、玩家之间公平（同一张图拼分数）、每天换一个种子就是"每日一图"的进阶结构。

## 7. 图形程序也要可测试

留一个 `?auto` 验证钩子：跳过开场、免伤、**以固定 dt 确定性预跑 N 帧**（不依赖真实时间流逝），再用 CDP 驱动 headless Chrome 截图并抓取页面 console。

**思路要点**：渲染类程序没有传统单测，但"确定性预跑 + 截图 + 埋点日志"就能构成回归验证——每次改动后跑到游戏中段任意位置，确认场景没崩、数值符合预期，比手工开游戏快得多，也能发现"正常玩根本遇不到"的角落问题。

## 8. 墙面渲染深度拆解：六层机制叠加出"手绘墙"

墙面"画感"不是单层噪声能做出来的，它是六层机制的叠加（按视觉贡献排序）：

### 8.1 手绘颜料图集（视觉贡献最大）

一张 3×3 的手绘水粉肌理图集（`gouache-atlas.png`），每种表面占一格：木板 [0,2]、挂板 [1,2]、路缘 [2,2]、路面 [0,1]、屋顶瓦 [1,1]、布料 [2,1]、灰泥 [0,0]、金属 [1,0]、草地 [2,0]（行/列）。着色器把表面 UV 映射到对应格子采样，以 **52% 强度乘进漫反射**（皮肤类 22%）——"画感"主要来自真人手绘的颜料纹理，程序化噪声只是补充。远处按屏幕导数（footprint）混合成该格 mip 平均色，防止闪烁。

### 8.2 每类表面的程序化法线 + ORM 贴图

为每种表面用数学公式实时生成 128×128 法线图与 ORM（AO/粗糙度）图，配方各不相同：挂板有水平凹槽（`cos(n·6π)^16` 窄沟）、木板有长木纹（`sin(t·9·2π)`）、屋顶瓦有逐行搭接（7 行 `^18` 窄带）。材质挂上 `normalMap`（强度 = 每类 relief 参数）、`roughnessMap`/`aoMap`——**墙面因此有真实几何起伏的光影**，挂板房墙面有一条条凹槽阴影。这是"质感"的另一半来源。

### 8.3 边缘"干笔描边"

顶点属性 `paintMask.r` 标记模型边缘。边缘不是均匀提亮——用高频噪声（`paintSpace*9`）经 `smoothstep(.32,.65)` 阈值化成**断续飞白的干毛笔笔触**，再向暖色（×1.16, 1.11, 1.025）提亮 43%。效果类似手绘勾线，且笔触在远处（表面 footprint 大时）自动淡出。

### 8.4 缝隙积色（AO 画进 albedo）

`paintMask.g` 标记凹槽/接缝，直接把 albedo 乘向冷灰（0.89, 0.93, 0.99），再叠加间接光压暗（`indirectDiffuse × mix(.58, 1, g)`）——窗框沟、墙角、板缝里有"积色"，暗部偏冷。

### 8.5 墙根竖向渐变

木板/挂板/灰泥墙按局部 Y 做渐变：`mix(vec3(.84,.87,.82), vec3(1), smoothstep(0, .72, y))`——越近地面越暗、越灰，模拟墙根阴影与尘土，让房子"落地"。玻璃另有竖向亮度渐变（底部暗蓝、顶部亮）。

### 8.6 噪声系统本身的讲究

- **每种表面独立参数表**：宏观频率（0.2~2.4）、笔触频率（meso，各向异性——挂板 [1.4, 12] 横纹、路缘 [1.5, 9]）、色漂幅度、笔触幅度全不同；
- **宏观色漂在世界空间**（房屋之间颜色自然衔接），**笔触在 UV 空间**（贴着表面），并用屏幕导数在远处淡出；
- hash 用无 sin 的高质量变体（`fract(p*.1031); p+=dot(p,p.yzx+33.33); fract((p.x+p.y)*p.z)`），避免 sin hash 的方向性条纹；
- 无 UV 的模型用**盒式投影**现场生成 UV（按法线主轴选投影面），配合 `defaultAttributeValues.paintMask` 兜底。

**一句话总结**：纯色墙面 + 单层噪声 ≈ 干净的塑料；手绘图集（纹理）× 程序化法线（起伏）× 干笔描边（勾线）× 缝隙积色（暗部）× 墙根渐变（落地感）× 参数化噪声（统一变化）= 手绘墙。
